version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
      - "4173:4173"
    environment:
      - NODE_ENV=production
      - VITE_API_URL=http://localhost:3001/api
    volumes:
      - task-data:/app
    restart: unless-stopped

volumes:
  task-data:
